# Amrutam-MedLink Back-end Server

This folder contains the Express user storage server (copied from the original `server` folder).

Run locally:

```bash
cd back-end/server
npm install
npm start
```
